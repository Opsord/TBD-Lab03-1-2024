name: OWASP Depedency Check Frontend

on:
  push:
    branches:
      - DevSecOps---David
  pull_request:
    branches:
      - DevSecOps---David

jobs:
  owasp-dependency-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16' # Usa la versión de Java que corresponda a tu proyecto
      - name: Install dependencies
        working-directory: Frontend
        run: |
          npm install  
      - name: Descargar OWASP Dependency-Check
        run: |
          curl -Ls "https://github.com/jeremylong/DependencyCheck/releases/download/v11.1.0/dependency-check-11.1.0-release.zip" --output dependency-check.zip
          unzip dependency-check.zip
          cd dependency-check/bin
          ls
          pwd

      - name: Ejecutar OWASP Dependency-Check
        run: |
          /home/runner/work/TBD-Lab03-1-2024/TBD-Lab03-1-2024/dependency-check/bin/dependency-check.sh --project "my-project" --scan /home/runner/work/TBD-Lab03-1-2024/TBD-Lab03-1-2024/Frontend/package-lock.json --out ./dependency-check-report --nvdApiKey ${{ secrets.NVD_KEY }} 

      - name: Subir el reporte como artefacto
        uses: actions/upload-artifact@v4
        with:
          name: dependency-check-report
          path: ./dependency-check-report